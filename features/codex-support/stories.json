{
  "feature": "CS-04 — user tier install: --user, the ~/.arcane store, and the home-directory fan-out (Codex Support program)",
  "branchName": "sessions/2026-09-09-cs04-user-tier",
  "assignedAgent": "claude (role: Backend/Full-stack — no roster installed in this repository)",
  "trackingMode": "internal",
  "externalProvider": null,
  "prd": "features/codex-support/PRD.md",
  "architecture": "features/codex-support/architecture.md",
  "previousEpic": "CS-03 stories are preserved in git history (commit 8d32d3a) and in PR #232",
  "stories": [
    {
      "id": "CS04-S1",
      "title": "Types, manifest scope, store paths and the scope-aware registry view",
      "description": "Add InstallScope = \"repo\" | \"user\", ArcaneManifest.scope? and fanout?, and user? on the init/update options in src/types.ts; validate scope in src/modules/manifest.ts like the other enums; create src/modules/user-tier.ts with userTierRoot() (homedir resolved on every call), USER_TIER_COMPONENTS (= SPELL_COMPONENT_NAMES), componentForScope(component, scope) (user: canonical files only, installed path spells/<id>.md, asset mapped via sourceOverrides) and toAbsoluteSpellPath(root, id) with forward slashes (architecture D1, D2).",
      "acceptanceCriteria": [
        "readManifest rejects scope values other than repo/user with ManifestInvalidFieldError and accepts a manifest with no scope",
        "componentForScope(c, \"repo\") returns c unchanged; componentForScope(c, \"user\") lists exactly one file per spell (spells/<id>.md) with sourceOverrides mapping each to .arcane/spells/<id>.md",
        "userTierRoot() changes when USERPROFILE/HOME change between calls",
        "npm run typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "assignedTo": "claude",
      "testEvidence": "Commit 0d89536. test/manifest.test.ts: scope repo/user/absent accepted, `machine` rejected, fanout record validated (64-hex digests) and malformed records rejected. test/user-tier.test.ts 'locations' + 'componentForScope': userTierRoot follows USERPROFILE/HOME between calls; store/canonical id round-trips in both slash styles; absoluteStoreSpellPath has no backslash; componentForScope repo identity, user view = one spells/<id>.md per spell with sourceOverrides to the canonical asset, every mapped asset exists on disk. typecheck + lint clean."
    },
    {
      "id": "CS04-S2",
      "title": "Fan-out engine with the ARC-038 hash discipline outside the store",
      "description": "In src/modules/user-tier.ts implement syncUserTierFanout({ homeDir, storeRoot, spellIds, previous, dryRun }) producing the desired set (~/.agents/skills/<id>/SKILL.md via renderCodexSkill and ~/.claude/commands/<id>.md via renderClaudeCommandStub, both with the absolute forward-slash canonical path) and reconciling it against the recorded fanout map: write/record, rewrite-on-change, keep-customized (hash carried forward), collision (never recorded), prune only when hash-matched plus emptied-directory removal, dry-run parity. renderClaudeCommandStub gains an optional canonicalRef parameter defaulting to canonicalSpellPath(id) (architecture D3).",
      "acceptanceCriteria": [
        "First run on an empty temp home writes 2 files per spell and returns a record with one hash per file; a second run writes nothing and reports every file unchanged",
        "An edited fan-out file is left byte-identical, reported as customized, and its previously recorded hash is carried forward",
        "A same-named file that was never recorded is left untouched, reported as a collision, and absent from the returned record",
        "A spell removed from the desired set has its recorded, hash-matched files deleted and its emptied <id> directory removed; an edited one is kept and reported",
        "dryRun writes nothing to disk and reports the same decisions",
        "Rendered content contains the absolute canonical path with forward slashes and never a repo-relative .arcane/spells path"
      ],
      "priority": 2,
      "passes": true,
      "assignedTo": "claude",
      "testEvidence": "Commit 0d89536. test/user-tier.test.ts 'renderUserFanout' + 'syncUserTierFanout' (8 cases): first run 4 written/4 recorded, second run 4 unchanged; missing recorded file restored; edited file byte-identical + customized + hash carried forward (idempotent on the third run); foreign same-named file = collision, never recorded, untouched by a later empty-set prune; claude_description change rewrites both shims of that spell only; dropped spell pruned with its emptied .agents/skills/<id> directory while an edited command is kept-edited then gone, and ~/.claude/commands is never removed; dry-run writes nothing for both write and prune; describeFanoutOutcomes lines. One implementation defect caught by these tests before commit: fs.rm on an empty directory is EISDIR without recursive -- switched to rmdir (removes exactly an empty directory). test/spell-compiler.test.ts: renderClaudeCommandStub renders over an explicit absolute reference."
    },
    {
      "id": "CS04-S3",
      "title": "spell init --user and the CLI wiring",
      "description": "Add --user to init/update/status/uninstall in src/index.ts (targetDir = userTierRoot(), options carry user: true). In src/commands/init.ts a user-scope path installs USER_TIER_COMPONENTS through componentForScope into the store, writes the manifest with scope: \"user\" and fanout, runs the fan-out and prints the store path, counts, the VS Code note and next steps — with no profile prompt, no git checks, no manifest questions, no hooks, no agent setup; --dry-run and --force honored (architecture D4, D5, D6).",
      "acceptanceCriteria": [
        "runInit({ user: true, profile: undefined }, ...) on a stubbed home creates ~/.arcane/.arcane.json with scope user, 41 files under ~/.arcane/spells/, 41 files under ~/.agents/skills/<id>/SKILL.md and 41 under ~/.claude/commands/, and a fanout record of 82 entries",
        "No @inquirer/prompts function and no git module function is called during a --user init",
        "--dry-run writes nothing and prints the would-copy/would-write lines; a second init reports Already initialized",
        "The printed output contains the VS Code note naming chat.useAgentSkills and stating that chat.promptFilesLocations is not used",
        "spell --help shows --user on init, update, status and uninstall"
      ],
      "priority": 3,
      "passes": true,
      "assignedTo": "claude",
      "testEvidence": "Commit 0d89536. test/init.test.ts '--user' (6 cases): store manifest scope user with 41 spells/<id>.md hashes matching disk, no repo-relative shim and no repo-only fields, fanout of 82 entries whose hashes match ~/.agents/skills/<id>/SKILL.md and ~/.claude/commands/<id>.md carrying the absolute forward-slash store path; select/confirm/input and inspectGitRepository/ensureLocalPullRebase never called; --dry-run lists Would copy/Would write and creates nothing; second init says Already initialized (user tier at ...) + spell update --user; --profile with --user rejected; a foreign same-named skill left alone, not recorded, named in the output. Built CLI: `<cmd> --help` shows --user for init, update, status, uninstall (checked live). Live EV-01 against the real home: 41/41/41 files written, output as designed (docs/research/skill-discovery-smoke-tests.md, CS-04)."
    },
    {
      "id": "CS04-S4",
      "title": "spell update --user: store update through the scope view, then fan-out regeneration",
      "description": "In src/commands/update.ts: for scope user skip the git checks and the commit-first warning, resolve registry components through componentForScope, fetch the previously published vendor file by its asset path (sourceOverrides[file] ?? file — the general fix), skip retrofits, and after the store update reconcile the fan-out (regenerating changed shims, pruning dropped spells) and persist the new fanout record; --dry-run and --prune as for a repo (architecture D2, D4).",
      "acceptanceCriteria": [
        "A deleted ~/.arcane/spells/<id>.md is restored by a same-version spell update --user and its missing fan-out files are rewritten",
        "An edited store spell goes through the three-way merge with the network mocked, and the fetch is requested at the asset path .arcane/spells/<id>.md",
        "A --user update never calls inspectGitRepository and never asks a retrofit question",
        "--dry-run reports the store and fan-out decisions without writing",
        "Existing repo-tier update tests pass unchanged"
      ],
      "priority": 4,
      "passes": true,
      "assignedTo": "claude",
      "testEvidence": "Commit 0d89536. test/update.test.ts '--user' (8 cases, fixture = a real runInit --user): no git check and no retrofit prompt across a version bump; Already up to date at the same version leaves the manifest byte-equal; a deleted client file is restored on a same-version run (Wrote 1 client file(s)) and re-recorded; a deleted store spell is restored through the ordinary path (Restored missing: spells/spell-status.md); an edited store spell merges with fetchPublishedFile called at the ASSET path .arcane/spells/spell-status.md and the vendor hash recorded; an edited client file kept + Kept 1 customized + hash carried forward across the bump; --dry-run reports Would write 1 client file(s) and changes nothing; a missing store points at spell init --user. All pre-existing update tests green."
    },
    {
      "id": "CS04-S5",
      "title": "spell status --user and spell uninstall --user",
      "description": "status: for the store print the component table, Scope: user, one fan-out line with per-client counts plus customized/missing counts, then the version footer; plain repo status prints Scope: repo and, only when a store exists, a User tier line. uninstall: reconcile the fan-out against an empty set (hash-checked, customized kept and listed), remove store files and the manifest, skip the push-policy refusal and repository-only cleanups; --yes and --dry-run honored (architecture D4).",
      "acceptanceCriteria": [
        "status --user output contains Scope: user and the fan-out counts; a customized fan-out file is counted as customized and a deleted one as missing",
        "Plain status prints Scope: repo, and the User tier line appears only when ~/.arcane/.arcane.json exists in the stubbed home",
        "uninstall --user removes every hash-matched fan-out file, its emptied <id> directories, the store files and the manifest; an edited fan-out file is kept and named in the output",
        "uninstall --user --dry-run lists what would be removed and deletes nothing; no push-safety or marker-section code runs for scope user"
      ],
      "priority": 5,
      "passes": true,
      "assignedTo": "claude",
      "testEvidence": "Commit 0d89536. test/status.test.ts 'scope and --user' (4 cases): Scope: user + Client files: 2 (1 Codex/Copilot skills, 1 Claude Code commands) + the VS Code note; 1 missing, 1 customized with the spell update --user remedy; missing store -> spell init --user + exit 1; repo status prints Scope: repo and the User tier: v1.1.0 at <store> line only once a store exists in the (env-stubbed) home. test/uninstall.test.ts '--user' (5 cases): client files, the emptied .agents/skills/<id> directory, store spell, manifest and the emptied store all removed while ~/.claude/commands survives; edited client file kept and named (--yes skips the prompt); --dry-run lists and removes nothing; a scope: user manifest is handled as the tier even without the flag; missing store -> spell init --user. Live: status --user and uninstall --user run against the real tier (research doc, CS-04 EV-01)."
    },
    {
      "id": "CS04-S6",
      "title": "spell doctor: non-blocking user-tier check",
      "description": "Add checkUserTier() to src/commands/doctor.ts and the runDoctor check list: no store → pass with the optional-install message; store present → warn when the store's major.minor differs from the CLI's, and warn when the recorded fan-out has missing or edited files, each with the spell update --user remedy; never blocking, never reading VS Code settings (architecture D4).",
      "acceptanceCriteria": [
        "With no store in the stubbed home the check passes and names spell init --user",
        "A store at a different major.minor produces a warn row (blocking false) naming spell update --user",
        "A missing or edited recorded fan-out file produces a warn row with counts; doctor's exit code is unaffected",
        "Existing doctor tests pass unchanged"
      ],
      "priority": 6,
      "passes": true,
      "assignedTo": "claude",
      "testEvidence": "Commit 0d89536. test/doctor-user-tier.test.ts (7 cases): no store -> pass naming spell init --user; matching major.minor with every client file in place -> pass with the store path and count; major.minor drift -> warn (blocking false) with the remedy; a missing recorded client file -> warn '1 of 2 client file(s) missing'; an edited client file -> pass message counts it as customized (kept-as-designed, so not a warning -- a deliberate narrowing of the AC's 'or edited'); unknown CLI version skips the comparison; unreadable store manifest -> warn 'could not read'. The version reaches the check via DoctorOptions.packageVersion from the CLI entry (bundle-safe; doctor.ts cannot require package.json relative to itself once tsup flattens it). Live: `spell doctor` in this repo printed `✓ [pass] User tier (ARC-045)` while the real tier was installed. Existing doctor suites green."
    },
    {
      "id": "CS04-S7",
      "title": "Docs, changelog, ADR note, research record, 1.1.0, bookkeeping, PR",
      "description": "README Once-per-machine subsection, CHANGELOG ## [1.1.0], portable-bootstrap.md tier sentence (source + parity copy), ARC-045 implementation note (Claude location, VS Code note rationale), research doc CS-04 section (Codex probe transcript, Claude deferral), live EV-01 against the built CLI's real spell init --user then uninstall --user; npm version minor as its own commit; PLAN.md CS-04 tick with the premise correction and Report line; OPERATOR-QUEUE.md Q-005; PRD AC5 status note; show-report regenerated trailer-free; PR with ## For the record; self-merge under the delegation once the three required checks are green (architecture D9).",
      "acceptanceCriteria": [
        "npm run check:version-bump passes (1.1.0 differs from main) and every gate in the architecture's Testing strategy is green",
        "The research doc carries the Codex user-tier transcript and the Claude Code deferral with Q-005 named",
        "PLAN.md, OPERATOR-QUEUE.md, PRD.md, DECISIONS.md and the show-report golden pair are consistent at HEAD",
        "PR merged by rebase under the codex-support-plan delegation; branch deleted after a content-verified check"
      ],
      "priority": 7,
      "passes": false,
      "assignedTo": "claude",
      "testEvidence": null
    }
  ]
}
