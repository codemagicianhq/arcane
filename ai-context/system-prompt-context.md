# AI Agent System Context

> **Keep this file current.** Update it at each session close via `/Spell-Close-Session`.

---

## Project Identity

| Field       | Value                                                                                                 |
| ----------- | ----------------------------------------------------------------------------------------------------- |
| Name        | Arcane (`arcane-cli`)                                                                                 |
| Description | Arcane framework CLI — scaffold and manage governance files in consuming repositories                 |
| Repo        | github.com/codemagicianhq/arcane                                                                      |
| Branch      | main                                                                                                  |
| Tracking    | Internal — persisted in `src/assets/.arcane.json` (this repo's self-hosted source manifest, `tracking_mode: "internal"`), per ARC-032 (2026-08-22). No root `.arcane.json` exists in this checkout; spells resolve the self-hosted source manifest as documented in `spell-open-session.prompt.md`. |

---

## Current Priorities

> **Staleness flag, added 2026-09-01 (not corrected — needs a real audit, not a guess):**
> item 2 below still names BC-01 as "next eligible," but `git log --oneline` on `main` shows
> BC-30 and BC-31 have already shipped (secret detection, vendor-neutral cicd-standards). This
> section was out of scope for the session that found the drift; re-audit `PLAN.md`'s actual
> current epic state before trusting the list below.

1. **The "Become Current" master plan is now the authoritative backlog.** [docs/plans/become-current/PLAN.md](../docs/plans/become-current/PLAN.md) (merged 2026-08-30, [PR #86](https://github.com/codemagicianhq/arcane/pull/86)) supersedes the ad hoc priority list below with 33 sequenced epics (BC-00..BC-32) covering every open `TODO.md` item, every unmarked `IDEAS.md` entry, all non-shipped intake findings, and every Accepted-ADR mechanism gap found by the 2026-08-30 audit — including items 2-5 below, which are now BC-01, BC-11, BC-18, and BC-03/BC-17 respectively. Its `OPERATOR-QUEUE.md` is the only surface where an autonomous loop asks the operator anything (platform mutations, ADR acceptances, content-holding-branch decisions); its `KICKOFF.md` is the per-iteration prompt. **The plan's standing delegation grant is now active** (activated by the PR #86 merge) — sessions executing its epics may commit/push/PR/merge within an epic's scope without per-action approval; this narrows (but does not replace) the general interactive-approval rule in `spell-commit-work.prompt.md`.
2. **Next eligible epic: BC-01 — the ARC-035 review-round merge gate.** [ARC-035](../DECISIONS.md#arc-035--auto-merge-requires-a-clear-review-round) (Accepted 2026-08-25) is still entirely unimplemented — `scripts/check-review-round.ts` does not exist, no spell posts a formal `--request-changes`, and the pre-push hook has no closed-PR warning. PLAN.md's BC-01 detail section is the spec; its step 1 is an empirical test (can a PR author self-`--request-changes`?) that must run on BC-01's own PR before the rest proceeds. Ruleset wiring itself is queued to `OPERATOR-QUEUE.md` Q-001/Q-002, not autonomous.
3. **ARC-020 stays Proposed; its remaining scope is folded into BC-11**, not tracked separately anymore. Two prior amendments (ARC-030, ARC-032) already resolved narrower slices.
4. **ARC-028 (concurrency and isolation model) is Accepted**, named **session workspace**. `0.22.1` is current on `main` (both version axes green as of 2026-08-30).
5. **Watch for the worktree-misdirect tool bug** (Edit/Write silently landing in the primary checkout instead of the active linked worktree — [journal/2026-08-22-ef-batch-closeout.md](../journal/2026-08-22-ef-batch-closeout.md)). Not recurred in recent sessions, but verify independently every time rather than trusting a clean streak.

---

## Next Session Handoff

> Auto-generated by spell-close-session. Consumed by spell-open-session. Do not edit manually.
> Generated: 2026-09-01

- **Active task:** None in progress. This session (`jolly-curran-505074-4e`, worktree
  `.claude/worktrees/jolly-curran-505074`) found, verified, fixed, and shipped the
  worktree test-infrastructure bug flagged in the previous handoff below: 11 test files
  hardcoded a `process.cwd()`-relative path to `dist/index.js` or `node_modules/tsx`, failing
  28 tests in any git worktree without its own `npm install`/`npm run build`.
  [PR #163](https://github.com/codemagicianhq/arcane/pull/163) merged, confirmed via
  `gh pr view 163 --json state,mergedAt,mergeCommit` → `MERGED` (`mergeCommit: 0d3ade78…`,
  `mergedAt: 2026-09-01T20:41:17Z`). This close-session commit (journal + this handoff + a
  small `TODO.md` PR-citation addition) is the only thing left to land.
- **Last completed step:** Independently re-verified PR #163's merge before writing this
  handoff — the operator's first "i merged it" report checked out **false** at that moment
  (`gh pr view` showed `state: OPEN`); surfaced this explicitly rather than proceeding with
  branch/worktree cleanup on the claim alone. The operator then genuinely merged it; a second
  check confirmed `MERGED` before this journal/handoff work began. Wrote
  [journal/2026-09-01-worktree-test-infra-resolve-cli-fix.md](../journal/2026-09-01-worktree-test-infra-resolve-cli-fix.md).
  About to open a `--docs-only` PR for this close-session commit itself.
- **Next concrete action:** Nothing blocking from this session. If resuming the become-current
  loop: **do not trust this file's "Current Priorities" list as-is** — re-audit
  `docs/plans/become-current/PLAN.md`'s actual current epic state first (flagged two sessions
  ago, still not corrected — out of scope for both).
- **Active files:** `journal/2026-09-01-worktree-test-infra-resolve-cli-fix.md`,
  `ai-context/system-prompt-context.md`, `TODO.md` — staged on
  `docs/session-close-2026-09-01-worktree-test-infra`, not yet merged.
- **Branch:** `docs/session-close-2026-09-01-worktree-test-infra` (created from a freshly
  fetched `origin/main`, which already includes PR #163's merge; named distinctly from the
  prior session's own `docs/session-close-2026-09-01` — that exact name was already taken,
  merged, by the time this session closed).
- **Blockers:** None.
- **Pending Verification:** None — the one pending item carried into this session (the prior
  handoff's own worktree-test-infrastructure finding, tracked below) resolved to `succeeded`.
- **Notes:** This session's worktree (`.claude/worktrees/jolly-curran-505074`) now holds a
  fully-merged, no-longer-checked-out branch `claude/jolly-curran-505074` that needs removal.
  Per `git-conventions.md`'s worktree rules, a worktree cannot safely remove itself — this must
  run from the **primary checkout** (`X:/Code/CodeMagician/products/arcane/arcane`, which
  holds `main`): `git worktree remove <this-worktree-path>`, then `git fetch --prune origin`,
  then `git branch -d claude/jolly-curran-505074`. Separately, the prior handoff below flagged
  a fully-merged `claude/gracious-dijkstra-a5a4bb` worktree/branch and one `+`-flagged,
  not-yet-resolved `docs/runnable-fences-selfhosted-agents` branch — neither touched this
  session, both remain exactly as that handoff described them.

<details>
<summary>Previous handoff (2026-09-01, WD-nn Phase 2 mislabeling fix session) — superseded, kept for the worktree/branch cleanup items it flagged that remain unresolved</summary>

- **Active task:** None in progress. This session found, verified, fixed, and shipped the
  WD-nn Phase 2 Audit table mislabeling bug (spell-make-discoverable vs
  web-discoverability-standards.md): [PR #161](https://github.com/codemagicianhq/arcane/pull/161)
  merged, confirmed via `gh pr view 161 --json state,mergedAt,mergeCommit` → `MERGED`
  (`mergeCommit: 3eaab13d…`, `mergedAt: 2026-09-01T20:08:30Z`).
- **Notes:** This worktree (`.claude/worktrees/gracious-dijkstra-a5a4bb`) holds a fully-merged,
  no-longer-checked-out branch `claude/gracious-dijkstra-a5a4bb` that needs removal. Per
  `git-conventions.md`'s worktree rules, a worktree cannot safely remove itself — this must run
  from the primary checkout (`X:/Code/CodeMagician/products/arcane/arcane`, which holds
  `main`): `git worktree remove <this-worktree-path>`, then `git fetch --prune origin`, then
  `git branch -d claude/gracious-dijkstra-a5a4bb`. Separately, `docs/runnable-fences-selfhosted-agents`
  (a local branch, unattached to any worktree) showed one `+`-flagged commit on a cheap
  `git cherry main docs/runnable-fences-selfhosted-agents` check — reported, not swept, since a
  `+` flag alone can be a `git cherry` false negative per that same doc's own caveat; needs the
  resulting-content check before any deletion decision.

</details>
