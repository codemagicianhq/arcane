# AI Agent System Context

> **Keep this file current.** Update it at each session close via `/Spell-Close-Session`.

---

## Project Identity

| Field       | Value                                                                                 |
| ----------- | ------------------------------------------------------------------------------------- |
| Name        | Arcane (`arcane-cli`)                                                                 |
| Description | Arcane framework CLI — scaffold and manage governance files in consuming repositories |
| Repo        | github.com/codemagicianhq/arcane                                                      |
| Branch      | main                                                                                  |

---

## Current Priorities

1. **Fix managed-prompt parity before expanding PR automation.** Run `spell-bug` on the HIGH source-vs-dogfood drift item in `TODO.md`; the root PR prompt currently says merge while canonical source and governance require rebase.
2. **Plan the open-PR synchronization workflow after the parity fix.** Run `spell-plan` then `spell-architect` for `spell-sync-pull-request`; do not implement it directly from the TODO.
3. **Continue the accepted intake backlog deliberately.** EF-20 remains the next intake research priority after the pinned PR-workflow items. The package is at `0.15.1`.

---

## Next Session Handoff

> Auto-generated by spell-close-session. Consumed by spell-open-session. Do not edit manually.
> Generated: 2026-08-02

- **Active task:** Fix the HIGH managed-prompt source-vs-dogfood parity defect, then plan `spell-sync-pull-request`.
- **Last completed step:** Confirmed PR #26 merged and rebased `sessions/2026-08-02-spell-compiler-idea` onto `origin/main` at `4543c12` (`v0.15.1`), with a recovery ref at `backup/session-spell-compiler-pre-reconcile-2026-08-02`.
- **Next concrete action:** Run `spell-bug` using the HIGH managed-prompt parity item in `TODO.md`; begin by reproducing the contradiction between `.github/prompts/spell-create-pull-request.prompt.md`, `src/assets/.github/prompts/spell-create-pull-request.prompt.md`, and `.arcane/governance/git-conventions.md`.
- **Active files:** `TODO.md`, `IDEAS.md`, `ai-context/system-prompt-context.md`, `journal/2026-08-02-spell-compiler-pr-sync-handoff.md`.
- **Branch:** `sessions/2026-08-02-spell-compiler-idea`.
- **Blockers:** None. PR #26 is merged. The current branch has been reconciled onto merged `main` and requires `--force-with-lease` when its closeout commit is pushed because its remote history still reflects the pre-reconcile ancestry.
- **Notes:** Keep the HIGH parity bug and MEDIUM `spell-sync-pull-request` feature in future handoffs until each is resolved or explicitly deferred. After the bug closes, run `spell-plan` to create `features/spell-sync-pull-request/PRD.md`, then `spell-architect`; use `spell-scope` only if planning proves the feature is too large. The broader spell-compiler entry remains an idea, not implementation authorization. No screenshots were provided.
