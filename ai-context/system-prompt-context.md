# AI Agent System Context

> **Keep this file current.** Update it at each session close via `/Spell-Close-Session`.

---

## Project Identity

| Field       | Value                                                                                                 |
| ----------- | ----------------------------------------------------------------------------------------------------- |
| Name        | Arcane (`arcane-cli`)                                                                                 |
| Description | Arcane framework CLI — scaffold and manage governance files in consuming repositories                 |
| Repo        | github.com/codemagicianhq/arcane                                                                      |
| Branch      | main                                                                                                  |
| Tracking    | Internal — persisted in `src/assets/.arcane.json` (this repo's self-hosted source manifest, `tracking_mode: "internal"`), per ARC-032 (2026-08-22). No root `.arcane.json` exists in this checkout; spells resolve the self-hosted source manifest as documented in `spell-open-session.prompt.md`. |

---

## Current Priorities

1. **The "Become Current" master plan is now the authoritative backlog.** [docs/plans/become-current/PLAN.md](../docs/plans/become-current/PLAN.md) (merged 2026-08-30, [PR #86](https://github.com/codemagicianhq/arcane/pull/86)) supersedes the ad hoc priority list below with 33 sequenced epics (BC-00..BC-32) covering every open `TODO.md` item, every unmarked `IDEAS.md` entry, all non-shipped intake findings, and every Accepted-ADR mechanism gap found by the 2026-08-30 audit — including items 2-5 below, which are now BC-01, BC-11, BC-18, and BC-03/BC-17 respectively. Its `OPERATOR-QUEUE.md` is the only surface where an autonomous loop asks the operator anything (platform mutations, ADR acceptances, content-holding-branch decisions); its `KICKOFF.md` is the per-iteration prompt. **The plan's standing delegation grant is now active** (activated by the PR #86 merge) — sessions executing its epics may commit/push/PR/merge within an epic's scope without per-action approval; this narrows (but does not replace) the general interactive-approval rule in `spell-commit-work.prompt.md`.
2. **Next eligible epic: BC-01 — the ARC-035 review-round merge gate.** [ARC-035](../DECISIONS.md#arc-035--auto-merge-requires-a-clear-review-round) (Accepted 2026-08-25) is still entirely unimplemented — `scripts/check-review-round.ts` does not exist, no spell posts a formal `--request-changes`, and the pre-push hook has no closed-PR warning. PLAN.md's BC-01 detail section is the spec; its step 1 is an empirical test (can a PR author self-`--request-changes`?) that must run on BC-01's own PR before the rest proceeds. Ruleset wiring itself is queued to `OPERATOR-QUEUE.md` Q-001/Q-002, not autonomous.
3. **ARC-020 stays Proposed; its remaining scope is folded into BC-11**, not tracked separately anymore. Two prior amendments (ARC-030, ARC-032) already resolved narrower slices.
4. **ARC-028 (concurrency and isolation model) is Accepted**, named **session workspace**. `0.22.1` is current on `main` (both version axes green as of 2026-08-30).
5. **Watch for the worktree-misdirect tool bug** (Edit/Write silently landing in the primary checkout instead of the active linked worktree — [journal/2026-08-22-ef-batch-closeout.md](../journal/2026-08-22-ef-batch-closeout.md)). Not recurred in recent sessions, but verify independently every time rather than trusting a clean streak.

---

## Next Session Handoff

> Auto-generated by spell-close-session. Consumed by spell-open-session. Do not edit manually.
> Generated: 2026-08-30

- **Active task:** None in progress. This session authored the "Become Current" master plan and its PR is merged: [docs/plans/become-current/PLAN.md](../docs/plans/become-current/PLAN.md) + `OPERATOR-QUEUE.md` + `KICKOFF.md`, shipped via [PR #86](https://github.com/codemagicianhq/arcane/pull/86) (merged `6021ca7`, confirmed via `gh pr view 86 --json state,mergedAt,mergeCommit` → `MERGED`). This close-session commit is the only thing left to land.
- **Last completed step:** Updated `ai-context/system-prompt-context.md`'s Current Priorities to point at PLAN.md as the authoritative backlog; writing this handoff and the session journal now.
- **Next concrete action:** Run the become-current loop per [KICKOFF.md](../docs/plans/become-current/KICKOFF.md) — `spell-open-session` with focus "become-current loop", then execute epic **BC-01** (ARC-035 review-round merge gate). BC-01's own detail section in PLAN.md specifies step 1 as an empirical test that must run first: on BC-01's own PR, attempt `gh pr review --request-changes` as the author and record the result in `DECISIONS.md` under ARC-035 — this is the flagged open question from the 2026-08-25 ADR, still unverified.
- **Active files:** `ai-context/system-prompt-context.md` — staged on this session-close branch, not yet merged. `docs/plans/become-current/{PLAN,OPERATOR-QUEUE,KICKOFF}.md` are already merged (PR #86).
- **Branch:** `docs/session-close-2026-08-30` (created per step 9a below before this commit).
- **Blockers:** None.
- **Pending Verification:** None. PR #86's merge was independently confirmed via `gh pr view --json state,mergedAt,mergeCommit` (not assumed from the earlier "I merged it" report) before this handoff was written; the push that created PR #86's branch ran the full local test suite via `.husky/pre-push` (699 passed, 2 skipped) before the PR was opened.
- **Notes:** **This session found and fixed a stale, previously-unconsumed handoff at session open** — the 2026-08-27 handoff (ARC-035 next-action) had never been marked consumed despite [PR #84](https://github.com/codemagicianhq/arcane/pull/84)/[PR #85](https://github.com/codemagicianhq/arcane/pull/85) landing after it; both were re-verified rather than trusted before proceeding. **PLAN.md's Authority & Delegation section is now active** (activated by the PR #86 merge, per the plan's own text) — it grants sessions executing a PLAN.md epic standing authority to commit/push/PR/merge within that epic's scope without per-action approval, narrower than but coexisting with the general interactive-approval rule in `spell-commit-work.prompt.md`. Revoking it means editing/removing that section on `main`. **Danger gates are deliberately NOT self-authorized**: any GitHub/ADO platform-settings mutation, deletion of a content-holding branch, manual publish, or ADR acceptance must go through `docs/plans/become-current/OPERATOR-QUEUE.md` — seeded today with Q-001 (ARC-035 ruleset wiring) through Q-005 (ARC-020 fold-in). Check that file periodically; it is the only thing the loop needs from the operator going forward.

> ✓ Consumed: 2026-08-31 — This handoff's "staged on this session-close branch, not yet merged" framing was stale by the time this session opened: [PR #87](https://github.com/codemagicianhq/arcane/pull/87) (branch `docs/session-close-2026-08-30`) merged 2026-08-31T07:02:14Z as `47fcd62`, confirmed both via `gh pr view 87 --json state,mergedAt,mergeCommit` and independently via `git log -1` on `main` matching that exact SHA — caught by this session's mandatory `spell-check-drift` pre-pass (EF-21), not assumed from the handoff text. Active task is None; next eligible work is BC-01 per `docs/plans/become-current/KICKOFF.md`.
