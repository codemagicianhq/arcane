# AI Agent System Context

> **Keep this file current.** Update it at each session close via `/Spell-Close-Session`.

---

## Project Identity

| Field       | Value                                                                                                 |
| ----------- | ----------------------------------------------------------------------------------------------------- |
| Name        | Arcane (`arcane-cli`)                                                                                 |
| Description | Arcane framework CLI — scaffold and manage governance files in consuming repositories                 |
| Repo        | github.com/codemagicianhq/arcane                                                                      |
| Branch      | main                                                                                                  |
| Tracking    | Internal — persisted in `src/assets/.arcane.json` (this repo's self-hosted source manifest, `tracking_mode: "internal"`), per ARC-032 (2026-08-22). No root `.arcane.json` exists in this checkout; spells resolve the self-hosted source manifest as documented in `spell-open-session.prompt.md`. |

---

## Current Priorities

1. **Batch-001 is down to one open item plus the explicitly-deferred docs-mode set.** EF-09 (push-safety) has a written PRD (`features/push-safety/PRD.md`) awaiting an operator decision before any implementation starts. Everything else independently actionable as of 2026-08-21 has shipped: EF-05, EF-08, EF-09(PRD), EF-13, EF-14, EF-20, EF-21, EF-22, EF-32, EF-33. Docs-mode (EF-03/04/07/10/11/12) and EF-18 remain deferred, untouched, gated on a live operator-design session — do not start these without that session.
2. **ARC-020 has now been amended twice, never Accepted.** ARC-030 (2026-08-21) resolved the inline-vs-separate-file question for venture/portfolio data (separate, `{business_root}/registry.json`); ARC-032 (2026-08-22) resolved it for tracking config (inline, on `profile`'s existing contract — `tracking_mode`/`external_provider` now persist in `.arcane.json`). ARC-020 itself carries an "Amended by" backlink to both and stays Proposed — its broader scope (operator identity, provider coordinates, repository lists) is still open. Treat this as an established pattern: a future incremental resolution of one more ARC-020 slice should amend it the same way, not flip it to Accepted prematurely.
3. **`spell-sync-pull-request`** (open PR branch synchronization) remains an open MEDIUM feature in `TODO.md`, not yet planned.
4. **Distribution-model research (2026-08-21)** landed as three `IDEAS.md` entries (`#distribution`, `#governance`, `#spell-compiler`) awaiting triage — MCP/marketplace/Skills/APM delivery channels, copier/`ng update` upgrade-safety, and single-source spell emit. Coordinate with the pending disclosure-model idea and ARC-028/029.
5. **ARC-031 (Accepted 2026-08-22)** widened the org-token gate into portability + repository-wide privacy layers and established the Ordovica/Tidewright/Overshore fictional venture family as the canonical example placeholder set. `v0.17.0` is current on `main`.
6. Merged-branch cleanup remains ancestry-based and is structurally defeated by ARC-009's sanctioned rebase-and-fast-forward — see the open `TODO.md` item under *PR Workflow and Prompt Integrity*. Unchanged since 2026-08-21; not touched this session.
7. **A live, reproducible tool bug was found and worked around 2026-08-22** (see [journal/2026-08-22-ef-batch-closeout.md](../journal/2026-08-22-ef-batch-closeout.md) and the persistent memory `tooling_edit_write_worktree_misdirect.md`): in a linked-worktree session, Edit/Write tool calls can silently apply to the *primary checkout* instead of the active worktree while reporting success. Verify consequential writes independently via Bash (`grep`/`stat`/`md5sum`) rather than trusting the tool's own success report, especially early in a session or after a long idle gap.

---

## Next Session Handoff

> Auto-generated by spell-close-session. Consumed by spell-open-session. Do not edit manually.
> Generated: 2026-08-22

- **Active task:** None in progress. This was the close of an autonomous overnight run covering WP5-WP7 (EF-22, EF-14+EF-08, EF-09); all three PRs merged, no implementation remains in flight.
- **Last completed step:** Merged [PR #58](https://github.com/codemagicianhq/arcane/pull/58) (EF-09 push-safety PRD, no version bump) via `gh pr merge --rebase`, independently confirmed `MERGED` with a real `mergeCommit` oid via `gh pr view --json state,mergedAt,mergeCommit`, remote branch deleted (`git ls-remote --heads` empty), primary checkout fast-forwarded to the same commit. PRs [#56](https://github.com/codemagicianhq/arcane/pull/56) (0.16.6) and [#57](https://github.com/codemagicianhq/arcane/pull/57) (0.17.0) merged and verified the same way earlier in this session. Full detail in [journal/2026-08-22-ef-batch-closeout.md](../journal/2026-08-22-ef-batch-closeout.md).
- **Next concrete action:** EF-09's push-safety design needs an operator decision before any further batch-001 work: read `features/push-safety/PRD.md` and either accept the `push_policy: blocked|guarded|open` design as-is, request changes, or resolve its own open questions (per-repo vs. per-directory granularity; non-CLI Git client hook support, flagged unverified; whether interactive-TTY-only is sufficient unblock-command agent-resistance). If the operator instead wants to keep going on something agent-executable without waiting on that decision, the next actionable item is triaging the three 2026-08-21 `IDEAS.md` entries (`#distribution`/`#governance`/`#spell-compiler`) via `spell-todo`/`spell-suggest-feature`.
- **Active files:** None uncommitted once this session-close PR merges (`journal/2026-08-22-ef-batch-closeout.md`, `ai-context/system-prompt-context.md`).
- **Branch:** `sessions/2026-08-22-ef-batch-closeout`.
- **Blockers:** None.
- **Pending Verification:** None — every dispatched action this session (three PR merges, three remote-branch deletions, three fast-forwards, CI checks on all three PRs) was independently confirmed `succeeded` via the GitHub API or direct git commands before being written up as complete anywhere (this handoff, the journal, `TODO.md`).
- **Notes:** `v0.17.0` is current on `main`. A live, reproducible Edit/Write tool bug was found and worked around this session (edits silently landing in the primary checkout instead of the active linked worktree) — see Current Priorities above and the persistent memory `tooling_edit_write_worktree_misdirect.md`; worth a quick sanity check early in the next linked-worktree session rather than assuming it was a one-off. Docs-mode (EF-03/04/07/10/11/12) and EF-18 remain the explicitly-deferred next topic, per the operator's own scope decision at this session's start — do not start them without a live design session. No screenshots were provided.
