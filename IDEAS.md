# Idea Inbox

Fast-capture log of ideas worth keeping. Add with `spell-save-idea`. Triage with `spell-todo`
(actionable) or `spell-suggest-feature` (product ideas). Entries are append-only; mark status in the
trailing comment rather than deleting.

---

- **[2026-08-01 00:11]** [#security] The org-token gate ([[docs/intake/batch-001/EF-26|EF-26]]) should catch the operator's own personal identifiers, not just org names - usernames, machine names, personal handles. Our own ai-context file has branch names containing a username sitting in a public repo. Whoever populates `ORG_TOKEN_PATTERNS` should seed it from the operator's identity, not just the org's. <!-- status: promoted → BC-27b 2026-08-31 -->
- **[2026-08-01 01:31]** [#tracking] GitHub should be a first-class `external_provider`, not `other`. Options are `ado | jira | other`, and `other` degrades to internal artifact flow with a TODO note. Arcane is published on GitHub, uses GitHub PRs, and `spell-bug` and `spell-create-pull-request` already speak GitHub natively - the tracking model is the only layer that does not. <!-- status: promoted → BC-09 2026-08-31 -->
- **[2026-08-01 19:22]** [#attribution] Split the `Agent` trailer into runtime versus persona: `Agent` = the runtime that executed (`copilot`, `claude`), `Persona` = the roster identity it operated as when applicable, `Role` = derived from that persona's roster entry and never typed by hand, plus `Model-Source` to mark self-reported values. Prerequisite: `.arcane/agents.yaml` must exist - it does not today, which is why persona attribution is unavailable and why EF-28 downgrades merge to human. Composes with EF-27's roster validation. <!-- status: promoted → BC-27d 2026-08-31 -->
- **[2026-08-02 01:25]** [#deduplication] Deduplication must diff before deleting. During housekeeping, compare duplicated blocks and merge any unique content before removing either copy; "duplicate" usually means drifted copies, not identical copies. <!-- status: promoted → BC-27a 2026-08-31 -->
- **[2026-08-02 02:05]** [#spell-compiler] Spell compiler, not spell runtime: the CLI should resolve and emit spells, not execute them. `spell render <name>` prints a fully resolved prompt to stdout and the agent client runs it, so `/spell-*` becomes a thin shim. This is EF-24's architecture-level answer: anything the CLI can compute stops being a rule an agent can get wrong. Render-time injection of `.arcane.json` config, governance paths, `BUSINESS_ROOT`/`SUBJECT_ROOT`, remote-state classification, and EF-27 validated roster identity would structurally defuse EF-02, EF-08, EF-14, EF-19, EF-23, EF-29, and fabricated author/model trailers. Explicitly not a runtime with API keys and conversations, because that means becoming the coding session. Key unresolved tension: `spell-authoring-standards` D2 Gold requires spells to work in a vanilla repo with no Arcane files present, and CLI-rendered spells may break that, so render/fallback needs a decision. Other risks: two sources of truth, CLI-vs-asset version skew, and migrating 33 client shims. **Partially addressed 2026-08-31 (BC-12):** [ARC-039](DECISIONS.md#arc-039--build-time-spell-compiler-generated-client-stubs-and-shared-prose-fragments) (`Proposed`) adopts the "compute it, don't restate it" compiler spirit for build-time structure (generated client stubs, shared prose fragments) but explicitly does NOT pursue runtime operator-config injection — the D2 Gold vanilla-repo tension this entry itself named has no resolution that doesn't either violate D2 Gold or accept real version skew, so EF-02/08/14/19/23/29 and fabricated-trailer defusal remain unaddressed. Left `status: new` rather than `promoted` since the harder half of this idea is deliberately out of scope, not done. **Build-time half shipped 2026-09-01 (BC-32):** ARC-039 accepted and implemented — `renderClaudeCommandStub()` generates every `.claude/commands/spell-*.md` stub from its prompt's frontmatter (closing 2 real title-drift bugs found live in the process: `spell-check-drift`'s stub had silently drifted to "Check Drift" against its prompt's "Check Doc Drift", and `spell-dotnet-expert`'s to "Dotnet Expert" against ".NET Expert"), plus a marker-based shared-fragment mechanism, both wired into `self-host-parity.ts` as two new parity axes. Runtime operator-config injection — the harder half this entry names — remains genuinely unaddressed; decision 3 scopes it out deliberately, not by oversight, so status stays `new`. <!-- status: new -->
- **[2026-08-02 02:05]** [#cli-ux] Immediate small fix regardless: `spell <unrecognized>` currently fails silently while the binary is named `spell` and 33 prompts are named `spell-*`; it should print "spells are prompts, run /spell-<name> in your agent client" and list the 6 real CLI commands. <!-- status: promoted → BC-08 2026-08-31 -->
- **[2026-08-02 16:30]** [#verification] Verification ledger — the missing artifact. The highest-value output of a working session is the corrections: claims that were checked and did not survive. `spell-close-session` captures what happened plus sanitized lessons learned; it throws away what was asserted, what was checked, what the check showed, and what changed. Those corrections are the evidence base for pattern findings — EF-21 and EF-24 both exist only because someone noticed the pattern across them — they are measurable as a calibration rate for the workflow, and they are how a new session inherits the behaviour rather than the personality. Proposal: a spell that extracts a verification ledger from a session on demand, separate from close-session. Confirmed instances from batch 001 alone: a 23-versus-6 file count stated confidently and used to justify a commit message; “all three pipeline templates” when there were four; EF-21's premise contradicted by its own source; fabricated Model and author trailers surviving eight PRs and human review; a dedup that deleted a lesson it assumed was duplicated; an operator's own "eight worktrees, seven prunable" claim, checked against the actual owning environment and found false — the worktrees were healthy, and the false read traced to a cross-filesystem mount rather than dishonesty on either side (see EF-33). Grading probe for whether a session inherited the protocol: “What does `Role: developer` in our commit trailers resolve from?” Plausible answer “the agent roster” is wrong; correct behaviour is to check, find no `.arcane/agents.yaml`, and report it is an example value at `git-conventions.md` line 554 that was never sourced. <!-- status: promoted → BC-27c 2026-08-31 -->
- **[2026-08-15 01:26]** [#governance] MCP fail-fast/fallback rule: one abnormal MCP tool failure (hang, idle-timeout abort, transport error, or empty response where data is expected) marks that server down for the rest of the session — all subsequent operations use the documented CLI fallback (`az repos pr` / `gh` / REST), no blind retries, and the downgrade is reported in output. Placement when authored: git-conventions near the Known-issues table + a section in agent-output.instructions + a spell-commit-work step-9 embed. Include consumer hardening advice: per-server `"timeout"` (ms) in `.mcp.json`. Origin: 2026-08-14 ops session lost ~1 h to two consecutive 30-minute MCP hangs before the documented `az` fallback was used. <!-- status: promoted → BC-22 2026-08-31 -->
- **[2026-08-15 01:26]** [#governance] Doc-ID link rule — the PR-link rule for documents: references to ADR-NNN / ARC-NNN / EF-NN / venture decision IDs / journal entries / named governance docs in agent output must be actionable — wiki-links inside repo markdown, clickable markdown links in chat/PR output, full canonical-repo URLs cross-repo. A bare `ADR-091` is the same defect as a bare `PR #NNN`; never fabricate an unresolvable link. While authoring, fix the found inconsistencies in the existing PR-link cluster: source-of-truth pointer precision in agent-output.instructions line 7 (points at "PR Standards section"; the row lives under its child "PR Requirements"), should→must alignment in the git-conventions PR Requirements row, Agent PR Workflow step 3's bare `[PR #{number}]` notification template, and the leaked ops-specific `ADR-048` citation in agent-output.instructions Merge Strategy. <!-- status: promoted → BC-06 2026-08-31 -->
- **[2026-08-15 01:26]** [#governance] Mechanical doc-ID link resolver: extend the `check:adr-references` CI gate to every ID class the doc-link rule covers (ADR/ARC/EF/journal/governance links) — IDs in shipped markdown must resolve to a real file+anchor or CI fails, same spirit as ARC-019's mechanical rejection of dead governance links. **Partially addressed 2026-08-31 (BC-06):** the gate now catches the ARC/EF cross-repo-hazard shape (a same-repo link to this repo's own `DECISIONS.md` from shipped content) — see `scripts/check-distributed-adr-references.ts`. Still open: full resolve-to-a-real-anchor checking for ARC-NNN/EF-NN (only the pre-existing ADR-NNN class gets that — cross-referenced against `framework-decisions.md`'s declared headings; a bare ARC/EF mention or an external URL has no local anchor to check), and journal-class links entirely (no reliable regex pattern found; zero real violations found while looking). **Parked 2026-09-01:** the ARC/EF-anchor and journal-link extensions remain genuinely unstarted — no epic has picked up this specific remainder since BC-06's partial fix; revisit if a real ARC/EF-anchor or journal-link violation is ever found (none has been, which is also why this hasn't been prioritized). <!-- status: parked — remainder unstarted, no violations found yet to motivate it -->
- **[2026-08-15 01:26]** [#assets] Link-hygiene residue in shipped assets: `src/assets/.arcane/governance/new-business-setup.md:116,121` wiki-link the nonexistent un-dotted `governance/git-conventions` path (the exact class ARC-019 rejects), and `spell-review-batch.prompt.md:74` / `spell-full-cycle.prompt.md:189` template/exemplify bare `PR #n` contrary to the PR-link rule. Fix in one pass, ideally caught by the resolver idea above. <!-- status: promoted → BC-06 2026-08-31 -->
- **[2026-08-15 01:26]** [#mcp] MCP config template / doctor check: Arcane ships no `.mcp.json` scaffold, so consumers get no per-server `timeout` and MCP hangs run to the client's 30-minute default idle limit. Consider an init/doctor check or optional template that seeds `"timeout"` per server — companion to the MCP fail-fast rule. <!-- status: promoted → BC-22 2026-08-31 -->
- **[2026-08-21 09:00]** [#distribution] AI-native delivery channels the copy-in model doesn't yet use — a 2026 landscape scan for shipping spells/agents/governance. Four channels now exist that the current dual-file copy-in predates: (1) **MCP prompts** surface as slash commands in both Claude Code and Copilot from one server — the only single-artifact both-client path for spells; (2) **Claude Code plugin marketplaces** distribute commands/skills/subagents/hooks/MCP as one versioned, auto-updating unit resolved outside the repo; (3) **portable Agent Skills** (`SKILL.md`) as an emerging cross-tool skill format; (4) **Microsoft APM** (Agent Package Manager) — a cross-tool dependency manager with a lockfile for exactly Arcane's payload across Copilot/Claude/Cursor/Codex/Gemini/Windsurf — the thing to ride or emit-for, not rebuild. Enabling finding: the "editor files must sit at fixed repo paths" assumption is false — Copilot honors `chat.promptFilesLocations`/`chat.agentFilesLocations`/`chat.instructionsFilesLocations` (arbitrary paths/globs/`~`) and Claude Code supports plugins + `~/.claude` + documented symlinks, so package-resident content can be referenced instead of copied (zero-drift by construction). Two smoke-tests before relying on it: whether Copilot's location globs traverse `node_modules` on Windows/macOS, and whether it follows a symlinked `.github/prompts`. Relates to the 2026-08-02 #spell-compiler idea (emit, don't copy) and to AGENTS.md, now a Linux-Foundation cross-tool standard Arcane already writes to. <!-- status: promoted → BC-28 2026-08-31 -->
- **[2026-08-21 09:00]** [#governance] Prior art for the still-open "customizations survive `arcane update`" thread ([[DECISIONS#ARC-019|ARC-019]] managed-standard-overrides follow-up, [[DECISIONS#ARC-020|ARC-020]], and the vendor-neutral customization backlog): the two mechanisms the ecosystem uses to update files a consumer has edited are **copier**'s git 3-way merge (regenerate-old → diff the user's edits → regenerate-new → merge; conflict markers only on real collisions; needs a recorded template version + clean tree, which `update.ts` already enforces) and **Angular `ng update`** migration schematics (versioned transforms that rewrite committed files via AST). Today `spell update` still does `copyFile(..., { force:true })` with no per-file hashes, so recovery leans entirely on git; a manifest that records per-file content hashes is the prerequisite for either a merge path or honest drift detection (also unblocks the `spell update --prune` orphan-report item). **Promoted 2026-09-01 (BC-31):** [ARC-038](DECISIONS.md#arc-038--content-preserving-updates-and-vendor-neutral-governance-content) implements exactly this — per-file SHA-256 hashing plus an npm-registry-backed three-way merge on `spell update`, and the `--prune` orphan-report item this idea named as unblocked. <!-- status: promoted → BC-11/ARC-038, implemented BC-31 2026-09-01 -->
- **[2026-08-21 09:00]** [#spell-compiler] Dual-copy elimination as a concrete driver for the 2026-08-02 spell-compiler idea: each spell ships as two independently hand-maintained sources — `src/assets/.claude/commands/spell-*.md` and `src/assets/.github/prompts/spell-*.prompt.md` (33 each) — and [[DECISIONS#ARC-027|ARC-027]]'s self-host parity guard only keeps each root copy in lockstep with its own `src/assets` source; it does nothing about commands-vs-prompts drift between the two client formats. A single canonical spell source that `spell render`/the compiler emits into both client shapes collapses 66 files to 33 + a generator and deletes this drift class outright — the same "compute it, don't restate it" logic already argued for the compiler. **Promoted 2026-08-31 (BC-12), premise corrected in the process:** verified directly against the real files rather than assumed — all 36 (not 33) `.claude/commands/spell-*.md` files are already 9-line thin shims using Claude Code's own `@`-file-inclusion to pull the real body from the `.prompt.md` source at invocation time, so body-content drift was never actually possible; the real, narrower gap was that these already-formulaic stubs are still hand-authored rather than generated. [ARC-039](DECISIONS.md#arc-039--build-time-spell-compiler-generated-client-stubs-and-shared-prose-fragments) closes that gap directly (a `renderClaudeCommandStub()` function, mirroring `agent-generator.ts`'s proven pattern, wired into self-host-parity as a new parity axis). **Shipped 2026-09-01 (BC-32):** the count grew again since ARC-039 was drafted — verified directly at implementation time: 41 prompt/stub pairs, not 36 — proceeded against the real, freshly-counted total rather than the ADR's now-stale figure. Decision 2 (shared prose fragments) also shipped, narrower than originally described: the 5 files named as duplicating a resolution-order paragraph "almost identically" turned out, checked directly, to share only a 2-line `tracking_mode`/`external_provider` declaration verbatim — the surrounding resolution logic had already independently diverged (e.g. `spell-scope` never checks root `.arcane.json` the way `spell-open-session`/`spell-plan` do). Extracted the fragment to match what is actually identical rather than forcing false uniformity across genuinely different logic; the deeper resolution-order inconsistency is a separate, disclosed finding, not silently fixed here. <!-- status: promoted → BC-12, shipped BC-32 2026-09-01 -->
- **[2026-08-30 03:06]** [#diagrams] Generated state diagrams — spells draw their own computed state as deterministic mermaid. Tier 1: the two-axis version check (`spell-open-session`/`spell-arcane-version`, later CLI `spell status`) already holds all three readings (repo-files version, installed CLI, npm latest) — emit a `gitGraph` with npm as `main`, the two branches forked at last-synced versions, gaps labeled axis A (`spell update`) / axis B (upgrade CLI); pure string template, no model judgment. Tier 2: topology/state reporters render from data already gathered (`spell-manifest` routing, `spell-review-batch` multi-PR topology, `spell-full-cycle` pipeline state, `spell-security-review` trust boundaries, `spell-commit-work`/`spell-create-pull-request` branch/PR gitGraph — zero prior art). Tier 3: harmonize existing diagram prescriptions (`spell-explain-concept`, `spell-architect`, `spell-scope`) under one convention with an applicability guard (no diagrams for one-line/speed-rule spells or simple definitions). Markdown-native: renders in VS Code chat, GitHub, and Obsidian; degrades to readable source in terminals; ADO wikis need the `:::mermaid` fence. Positioning: the methodology that draws its own state. Origin: arcane-website TODO 2026-08-30. <!-- status: promoted → features/generated-state-diagrams/PRD.md + ARC-036 2026-08-30 -->
- **[2026-08-30 03:06]** [#assets] New instance of the 2026-08-15 link-hygiene class: governance ADR links of the form `../../../DECISIONS.md` (e.g. `spell-authoring-standards.md:18` linking ARC-014) resolve to a nonexistent file from consuming repos — from `.arcane/governance/` the relative path escapes the consumer's repo root. Framework ARC references in shipped assets need the doc-ID link rule treatment (plain-text ID or full canonical URL); the mechanical resolver idea above would catch the class. <!-- status: promoted → BC-06 2026-08-31 -->
- **[2026-08-31 04:20]** [#governance] Hardcoded line-number citations in prose go stale silently as the target file grows. Found executing BC-07 (TODO.md T6's re-run): TODO.md's own citation of `.arcane/governance/git-conventions.md:554` for the `Role` commit-trailer example was correct on 2026-08-02, but the file grew by ~80 lines across six unrelated epics since (BC-01's review-round section, BC-03's Content-Verified Branch Deletion section, others) — the real content is intact, just now at line 635. Nothing flagged the drift; a reader following the citation lands on unrelated prose instead of an error, which is worse than a dead link because it doesn't announce itself. Distinct from the 2026-08-15/BC-06 doc-ID link class (that's a link resolving to the *wrong document*; this is a same-file citation resolving to the *wrong line*). A heading-anchor or nearby-unique-string reference survives edits that a bare line number doesn't; alternatively, a mechanical check could flag any `file.md:NNN`-shaped citation whose target file's line NNN doesn't contain a quoted/expected substring. Scope not assessed — how many other `*.md:NNN` citations exist repo-wide, and whether a cheap regex-based checker is feasible, are both open. **Parked 2026-09-01:** scope still not assessed; no epic has picked this up. Revisit alongside a real instance of stale-citation confusion, or when a session has spare capacity to survey how common this citation shape actually is repo-wide. <!-- status: parked — scope unassessed, no epic has claimed it -->
