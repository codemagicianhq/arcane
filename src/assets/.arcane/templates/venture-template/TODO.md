# Open Items — {Venture Name}

Per-venture todo book (hub-side). Add with `spell-todo` targeting this venture (or natural
phrasing, e.g. "add a todo for {slug}"); triage alongside the hub root list with `spell-todo
--sweep`. This is a scratchpad for unresolved items, not a system of record — when an item is
resolved, move the outcome to the venture's `overview.md` or `DECISIONS.md`, then delete it here.

---
