# Verification Ledger

A running record of claims that were checked during a session and what the check showed — appended by
`spell-verification-ledger`, run on demand, separate from `spell-close-session`'s narrative record.

The value here is the calibration signal: how often a checked claim survives (`confirmed`) versus gets
corrected (`corrected`) or can't be resolved (`unverifiable`). A `corrected` entry is evidence the
verification step did something, not a record to be embarrassed about.

This file starts empty — it is not backfilled with historical examples from before this spell existed,
since reconstructing them accurately would require re-deriving details this file's own discipline says
not to guess at. It captures new sessions going forward.

Schema per entry: **Claim** (as originally stated) · **Verification method** (what was actually done to
check it) · **Result** (`confirmed` / `corrected` / `unverifiable`) · **Correction** (for `corrected`
only — what the check showed and what changed).

---

## 2026-09-02 — Become Current corrections (2026-08-31 → 2026-09-02)

Scoped to verification events on or after this ledger's own creation (2026-08-31, BC-27c) — not
backfilling anything from before it existed.

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| TODO.md: "R3 (lifecycle-operations rule) is genuinely still open" | Re-grepped `universal-agent-rules.md` case-insensitively for "lifecycle operations" | corrected | The first grep was case-sensitive and missed rule 22's capitalized "**Lifecycle operations** run through their spell…" — R3 was already shipped. TODO item closed in full; error named on the record. |
| ARC-039 (I15's premise): "36 (not 33) spell command stubs exist" | `ls .claude/commands/spell-*.md` at BC-32 implementation time | corrected | 41, not 36 — the count grew again in the day between the ADR's drafting and its implementation. |
| ARC-039 decision 2: "5 files duplicate a resolution-order paragraph, worded almost identically" | Diffed the actual text of all 5 consuming files | corrected | Only a bare 2-line `tracking_mode`/`external_provider` enum is byte-identical; `spell-scope` skips the `.arcane.json` source entirely that the other four check — the surrounding logic had already diverged. Extracted only the genuinely-shared 2 lines. |
| ARC-039 decision 1: stub `description` is generated "from its frontmatter (`name`, `description`)" | Byte-compared all 41 stubs' `description` fields against their prompts' own `description` | corrected | Every stub carries a distinct hand-written "Use PROACTIVELY…" invocation hint, absent from the prompt. Added a new `claude_description` field instead of overwriting it. |
| TODO.md (own text, written 2026-09-01): "these seven are the only unchecked items" | Counted the actual bullets under the new heading | corrected | Eight bullets — a parent finding plus its own still-open sub-item, one topic but two checkboxes. |
| TODO.md (own text, written 2026-09-01): pointers read "(line 443)" / "(line 446)" / "(line 448)" | Re-read TODO.md's current line numbers during Lessons Hardening planning | corrected | Items had already moved to 449/452/454 — drifted within roughly 24 hours of being written, by the file's own subsequent edits. |
| PLAN.md (own text, BC-06 closure note): "added to this run's operating lessons below" | Grepped PLAN.md for a heading named "operating lessons" | corrected | No such section exists anywhere in the file. |
| README.md tagline: "38 spells" / "23 governance standards" | `ls` counts against the live registry and asset tree | corrected | 41 spells, 25 governance docs. |
| `ai-context/system-prompt-context.md`: "ARC-020 stays Proposed; its remaining scope is folded into BC-11" | Read `OPERATOR-QUEUE.md`'s Q-005 entry directly | corrected | Q-005 explicitly says NOT subsumed — different axis entirely (manifest data fields vs. governance-content architecture). |
| `ai-context/system-prompt-context.md`: "`0.33.1` is current on `main`" | Read `package.json`'s live version field | corrected | `0.33.2` — the claim was stale in the same PR that performed the bump to `0.33.2`. |
| `universal-agent-rules.md` rule 3: "no secret-scanner exists in this repo yet (tracked as future work, BC-30)" | Read `src/modules/secrets-scan.ts` and `.husky/pre-commit` directly | corrected | BC-30 shipped exactly this; the distributed governance doc was never updated after. |
| "The other session wrapped up" (assumed the full ARC-023 prompt/instructions pass) | `gh pr view 161/163 --json files` | corrected | Narrower scope than assumed (a WD-nn labeling fix + a worktree test-infra fix); the gate's real concern was satisfied regardless. |
| The org-token closure note (describing the leak's removal) was safe to write | CI's org-token lint run, twice on the same PR | corrected | The note itself quoted the real client name verbatim — once in the first draft, again in its own fix of that draft. Both replaced with the ARC-031 fictional-venture convention. |
| Every PR from PR #165 through #170 was merged | Fresh `gh pr list --state all --limit 10` immediately before writing the close-session journal | confirmed | — |
| `rca-process-standard.md` stores RCAs at `governance/rcas/` | Cross-checked against `portable-bootstrap.md`'s "do not create a duplicate root `governance/` tree" rule | unverifiable | Genuine contradiction between two governance docs, not resolvable by re-checking either one alone — routed to Lessons Hardening's LH-02 to resolve rather than guessed at here. |

## 2026-09-02 — Lessons Hardening corrections (LH-02 → LH-12)

Scoped to verification events during epic execution, not already captured in RCA-001's own Timeline
(the "three times" recurrence-count correction is logged there as CA-4; not duplicated here).

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| TODO.md's new ARC-014 citation, copied from ARC-031's own "Related" line, correctly points at the ADR for "org-token lint as a build gate" | `grep "^## ARC-014"` in `DECISIONS.md` while drafting ARC-041 (LH-11) | corrected | ARC-014's real title is "Spell Authoring Standards: A Quality Rubric for Spell Prompts" — unrelated to org-tokens. Pre-existing bug in ARC-031 itself, not introduced by LH-11. Fixed the new ADR to use plain prose instead of the copied link; routed the pre-existing bug as its own TODO.md item (root ADR number not yet identified). |
| `resolvePrivateTokens()`'s test suite passes after converting the function to `async` | Ran the actual test suite, not just `tsc --noEmit` | corrected | Typecheck stayed green, but two un-awaited call sites in `test/org-token-lint.test.ts` failed at runtime (`Received: Promise {}`; `TypeError: tokens.map is not a function`). Fixed both call sites with `await`. |
| The new "refuses a file inside a real repository" test exercises the in-repo refusal path against its own fixture repo | Traced the test's actual `cwd` at the call site against the implementation | corrected | `resolvePrivateTokens()` had no `cwd` parameter yet, so the check silently ran against `process.cwd()` (the real checkout) rather than the fixture — the test passed without exercising the refusal logic at all. Added an optional `cwd` parameter threaded through to `readLocalTokenFile`; test now passes the fixture dir explicitly. |
| `registry.test.ts`'s `toHaveLength(25)` on the `governance-only` profile is correct because the profile and the count agree | `comm` diff of the profile's real file list against `src/config/profiles.ts`'s declared list (LH-05 empirical-first step) | corrected | The profile was missing `records-conventions` while wrongly including `agent-output-instructions` — two miscounts canceling out to the same total. Fixed `profiles.ts`; rewrote the test to derive its expectation from the registry instead of a literal. |
| `check-citations.ts`'s first design (flag every bare `path` citation lacking a stable locator) is ready to run in report mode | Ran it against the live tree in report mode (LH-07) | corrected | 452 findings, nearly all false positives — flagged any backtick-wrapped filename in prose, not just citations making a specific claim. Narrowed to citations carrying an anchor, quoted phrase, or line number; findings dropped to 30 genuine ones. |
| The narrowed citation regex catches every bare line-citation in the living-docs set | Re-ran `check:citations --report` after narrowing, diffed against a manual scan (LH-07) | corrected | Comma-separated line lists (e.g. a file cited at two lines like "7,115") weren't matched by the original `:NNN`/`:NNN-MMM` pattern. Broadened to `(?::(\d+(?:[-,]\d+)*))?`, using `Math.max()` over the split numbers for the line-count bound. |
| A paragraph-level window is sufficient for `check-followups.ts` to associate a deferral phrase with a nearby tracker token | Measured the character distance between the known true positive's deferral phrase and its tracker token inside the real historical paragraph (LH-09) | corrected | The true positive sat 314 characters from an unrelated "ARC-023" mention inside one 12,674-character paragraph — a paragraph-level check would match the wrong token and produce a false negative on the known case. Fixed with a 150-character window, verified directly against the historical true positive. |
| The tracker-token pattern (`TODO\.md`/`IDEAS\.md`, requiring the literal `.md`) matches every real tracker reference in the corpus | Ran the check against the known true-positive corpus (LH-09) | corrected | Missed "Filed a separate follow-up TODO item" (bare word, no `.md`). Broadened to `\bTODO\b\|\bIDEAS\b`. |

## 2026-09-03 — Show Report SR-00 through SR-03

Scoped to verification events during the session that activated the Show Report program and shipped
its first four epics. Four of the seven checks corrected the claim they tested.

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| SR-02's generated Lessons Hardening report is correct; its committed stat rail reads `0.33.2 → 0.34.2` | Rendered the page and read it against the hand-built ledger and `docs/research/show-report-feasibility.md`'s verified figures | corrected | True close is `0.34.1`. `sources.ts` had taken "the last commit touching PLAN.md" as the close commit, and SR-01's own backfill had just edited that finished plan. Close-commit derivation replaced. |
| "Now bounded by the plan's `completed:` date via author calendar dates; LH reads `0.33.2 → 0.34.1` again, matching the hand ledger and the feasibility memo" | Regenerated **both** programs after the fix, not only the one that had been wrong | corrected | Become Current broke to `0.33.0`: its last PLAN.md-touching commit on its completed day sat at 0.33.0 while two version bumps landed later the same day without touching the plan, and author dates are not monotonic along a rebase-merged log. Redefined as "the commit `main` stood at when the `completed` day ended," by committer (landing) date `%cs`. |
| PR #195's CI passed — the background watch reported "completed (exit code 0)" | Read the watch command's actual output file, then re-ran `gh pr checks 195` live | corrected | The watch had exited in under a second printing `no checks reported on the '<branch>' branch`; all three checks were still `pending`. Merging on that summary would have been an unverified merge. Every subsequent watch used a readiness loop (poll until a `pass\|fail\|pending` line exists) plus a live re-read before merging. |
| `v0.34.3`'s publish failed because `publish.yml` checks out shallow while `ci.yml` uses `fetch-depth: 0`, and the golden parity test derives the version span and cast from git history | `gh run view --log-failed` on the failed run, compared checkout config across `publish.yml`/`ci.yml`/`release-drift.yml`, then tested `isShallowRepository()` against a real `git clone --depth 1` and confirmed `arcane-cli@0.35.0` reached npm after the fix | confirmed | — |
| The backfill agent's self-report: "become-current 33/33 ✓, lessons-hardening 14/14 ✓, bare PR-citation backfill: 0 fixes needed" | Ran this session's own `parseEpics` over both files independently of the agent's claim, plus a grep for unlinked `PR #NNN` citations | confirmed | — |
| `sessions/2026-09-03-ai-discoverability-rules` is fully landed and safe to delete — `git cherry` shows 0 unlanded commits | A three-dot diff showed 182 insertions, so followed with a per-file two-dot diff against `main` and a `git log --grep` for both commit subjects | confirmed | — |
| `docs/plans/show-report/OPERATOR-QUEUE.md` Q-001 read `- **Status:** [ ] open` | Checked PR #192's real state with `gh pr view` while closing Q-002 | corrected | It had merged hours earlier. The same static-status drift (P1) Lessons Hardening's own Q-001 exhibited, caught the same way; marked done with the merge SHA recorded. |

## 2026-09-09 — Codex Support CS-03 (canonical spell move) and the CHANGELOG catch-up

Scoped to verification events during the session that landed the CHANGELOG catch-up (PR #231) and
shipped CS-03 for the operator's merge (PR #232, `1.0.0`). Nine of the twelve checks corrected the claim
they tested — two of them statements this session had already put on the record.

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| "The CHANGELOG catch-up is 0.23.0 → 0.39.0" (the brief given to the research agent) | `git tag --list` range listing plus `npm view arcane-cli versions` filtered from 0.22.1 | corrected | Fourteen published `0.22.x` releases (BC-01 through BC-17) sat outside the brief; written from their commits. The file now covers 0.22.1–0.39.0, 60 entries, every version cross-checked against npm. |
| "`v0.34.3` is a release like the others" (a `## [0.34.3]` heading in the draft) | `npm view arcane-cli@0.34.3` (E404) and `gh run list --commit 2564110` (Publish: failure) | corrected | Tagged and GitHub-released, never on npm — the shallow-clone publish failure SR-03 fixed. Folded into 0.35.0's entry with the cause. |
| "All 60 CHANGELOG dates match their tags — `dateMismatch: []`" (this session's own verification script) | The script's `noTag` side field listed every version; `execSync` runs under cmd.exe on Windows, where single quotes are literal, so the tag map was empty; re-run with double quotes | corrected | The first pass compared nothing. The re-run compared 60 and found 0 mismatches — the claim became true only once the check itself was fixed. |
| "Today's `spell update` conflicts on a hand-edited prompt when its vendor file becomes a shim" (CS-03's premise as planned) | `runUpdate` against a real 0.39.0 consumer fixture, three variants, merge bases fetched from real npm history | corrected | An appended edit three-way merged "successfully" into the new shim with the edit dangling underneath, reported as `Merged your edits`; an in-body edit left conflict markers over a vanished body; an entry with no recorded hash was overwritten silently. Design kept; premise rewritten in PLAN.md and ARC-045's implementation note. |
| "Overrides survive `arcane update`" (portable-bootstrap.md, ARC-038) | Two consecutive updates through the merge path against real published history (variant D) | corrected | The edit survived exactly one update — after a merge the merged file's hash was recorded as "what Arcane last wrote". Fixed by recording the vendor content's hash (`0c5a47b`). |
| "`git mv` preserved history" (commit `8a80759`) | `git log --follow` on `src/assets/.arcane/spells/spell-plan.md` versus the old path | confirmed | — |
| "Codex follows the shim to `.arcane/spells/<id>.md`" | `codex exec --sandbox read-only` in a consumer installed from the 1.0.0 build; the stderr tool trace | confirmed | — |
| "Claude Code's `@` include resolves the relocated file" | Invoking `spell-status` through the regenerated `.claude/commands` stub in this session | confirmed | — |
| "The background codex run is slow, not stuck" | Read its stderr; `Get-CimInstance Win32_Process` | corrected | `Reading additional input from stdin...` — `codex exec` blocks on stdin from a background shell. Killed by command-line match; re-run with `< /dev/null` finished in under a minute. |
| "`check:citations` and `check:followups` report no new findings" (commit `649d4ce`'s message) | The full `check:citations` listing rather than its last line | corrected | Six living-doc citations (DECISIONS.md, IDEAS.md, TODO.md) broke because the old spell filenames now resolve to shims; retargeted in `051a6ea`. The commit message stands on the record as written, and it was wrong. |
| "A same-version run restores missing tracked files and reports them … nothing more" (architecture D5.2) | The independent reviewer's scratch probe of `runUpdate` at the same version | corrected | It wrote every absent registry file of the component — six files for two counted. The write set now equals the gate's set, with user-owned files excluded (`78bbebc`). |
| "There is deliberately no second copy of the shim path shapes anywhere else" (a comment in `spell-compiler.ts`) | The reviewer's read of `SHIM_TARGETS` in `self-host-parity.ts` | corrected | The parity generators duplicated the three shapes with nothing binding them to the matcher; `CLIENT_SHIM_PATHS` now lives beside the matcher, consumed by the targets and bound by test (`78bbebc`). |

## 2026-09-10 — Codex Support CS-04 (user tier install)

Verification events from the CS-04 session (opened 2026-09-09, shipped 2026-09-10 as `1.1.0`,
[PR #234](https://github.com/codemagicianhq/arcane/pull/234)). Two rows correct a premise the plan and
the accepted ADR were written on; four correct the executing agent's own working assumptions, caught by
its tests or by the independent Phase 5 review; one is genuinely unverifiable from an agent session and
is routed to the operator rather than assumed.

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| PLAN.md CS-04 / ARC-045 decision 3: the user tier prints a `chat.promptFilesLocations` settings snippet for VS Code Copilot | Fetched VS Code's AI settings reference and agent-skills documentation (2026-09-09) before designing | corrected | All four `chat.*FilesLocations` settings are deprecated ("This setting and the Local agent will be removed in a future release"); Copilot discovers `~/.agents/skills` by default and lists skills under `/`. The tier prints a note that no setting is needed and never touches `settings.json`. Recorded as an ARC-045 implementation variance. |
| ARC-045 decision 3 (letter): the Claude Code fan-out goes to `~/.claude/skills/<id>/SKILL.md` | Same documentation: Copilot's default skill locations include `~/.claude/skills`; Claude Code's documentation: `.claude/commands/<name>.md` and `.claude/skills/<name>/SKILL.md` "both create `/deploy` and work the same way" | corrected | A skill there would list every spell twice in Copilot's picker. The Claude file is a command at `~/.claude/commands/<id>.md`, outside Copilot's scan set. Recorded as the second ARC-045 implementation variance. |
| Handoff note: "the CS-04 fan-out must not use `copier.ts` (its target-path guard forbids leaving the target directory)" | Read `src/modules/copier.ts ("export function validateTargetPath")` | confirmed | — |
| Design assumption: `os.homedir()` follows `USERPROFILE`/`HOME` at call time, so tests can stub the environment instead of mocking the function | Ran node with both variables overridden mid-process and printed `os.homedir()` | confirmed | — |
| Codex follows a user-level skill to an absolute path outside its working directory | Live `codex exec --cd <empty dir>` against a probe skill in `~/.agents/skills/`, then again against the real `spell init --user` output; the trace inspected for the reads | confirmed | — (marker returned both times; two `Get-Content -Raw` reads, no path rewriting) |
| Claude Code resolves a personal command's absolute `@` include from any working directory | Wrote a probe command to `~/.claude/commands/`; the running session's Skill tool reported it unknown (list fixed at startup) and a nested `claude -p` was not logged in (a separate credential store, not worked around) | unverifiable | Documentation says `@` paths "can be relative or absolute" and personal commands are available across all projects; recorded as documentation, not observation, and routed to the operator (`docs/plans/codex-support/OPERATOR-QUEUE.md` Q-005). The stub carries the read-and-follow sentence as well as the include. |
| Test premise (own): changing a store spell's `description` changes both rendered user-level shims | Ran the test: the Codex and Claude renderers both prefer `claude_description`, which every shipped spell carries | corrected | The test now changes `claude_description` and asserts both shims are rewritten while the other spell's stay unchanged. |
| Implementation assumption (own): `fs.rm(dir, { recursive: false })` removes an emptied per-spell directory | The prune test failed: `rm` returns `EISDIR` for a directory without `recursive`, and the swallowed error left the directory behind | corrected | `removeDirectoryIfEmpty` uses `rmdir`, which removes exactly an empty directory and refuses a non-empty one. Caught before the feature commit. |
| Architecture D3: "Dry-run prints the same decisions with a `[dry-run] Would …` prefix and writes nothing" | The independent reviewer's reproduction: `rm ~/.arcane/spells/spell-status.md; spell update --user --dry-run` | corrected | Unhandled `ENOENT` with no preview, on both the same-version and the version-change path — the fan-out rendered from the store before anything was restored. Fixed: a missing store copy renders from the vendor asset (`fallbackDir`); a spell with neither source is reported `unrenderable`. Tests for both paths (review F1). |
| Architecture D4: `checkUserTier` is "non-blocking, always" | The reviewer's reproduction: a store manifest with no `version`; a recorded key resolving to a directory | corrected | Both threw out of `runDoctor`'s `Promise.all`, crashing `spell doctor` in every repository on the machine. Fixed: a missing version is one warn row, the inspection is wrapped, and every fan-out path is `lstat`-classified before it is hashed (review F2, F7). |
| Implicit design assumption: a manifest with `scope: "user"` is always the store, so `homeDir = dirname(targetDir)` | The reviewer's scenario: `mv ~/.arcane ~/backup/.arcane; cd ~/backup/.arcane; spell update` | corrected | 82 shims would land under `~/backup/.agents` and `~/backup/.claude`. Fixed: without `--user`, a user manifest is honored only when the directory is `~/.arcane`; anywhere else `update`/`status`/`uninstall` refuse with a message (review F4). |
| Test claim (own): "`update --user` runs no git check and asks no retrofit question" proves the retrofit skip | The reviewer's read of the gate: the wizard is TTY-gated and vitest has no TTY, so the prompt mocks go uncalled for a repository manifest too | corrected | Vacuous as written. The test now forces `process.stdin.isTTY = true` (review F5). |
| Commit `fd588d0`: fan-out keys that could escape the home directory are rejected when the manifest is read | The reviewer's module-level reproduction at the earlier commit (a `../victim` key deleted a file outside home) and re-check at HEAD | confirmed | — (closed before the review finished; the reviewer's further suggestion to restrict keys to the two known shapes was declined on forward-compatibility grounds, recorded in the PR) |
| "A merged version bump is a published one" (the 0.34.3 lesson, applied as a check rather than assumed) | `gh run view` on `publish.yml` run 34497846257, `npm view arcane-cli version time.modified`, and the `1.1.0` tarball unpacked and inspected (`dist/index.js` carries the user tier; `init --help` lists `--user`) | confirmed | — (release `v1.1.0` published 2026-09-10T15:46:33Z, npm `1.1.0` at 15:47:27Z) |

## 2026-09-10 — The 1.1.1 findings release, Q-005 prepared, and the CS-08 spike

Same conversation as the CS-04 section above, continued at the operator's direction ("let's fix all
low medium and high findings, and continue with q-005 and cs-08"). Three of the executing agent's
own working claims were corrected by its own checks, one by the regression it wrote; one verification
method turned out to be vacuous and was redone; one failure went uncaptured and is recorded as such.

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| "The published 1.1.0 CLI's help lists `--user`" — checked with `npx --yes arcane-cli@1.1.0 init --help \| grep -c -- --user`, which printed `0` | Unpacked the tarball, then `npm install arcane-cli@1.1.0` into a scratch prefix and ran `dist/index.js <cmd> --help` for all four commands | corrected (method) | The first check was vacuous, not a product failure: the package's bins are `spell` and `arcane`, none named after the package, so `npx arcane-cli` never ran the CLI. The real install lists `--user` on `init`, `update`, `status` and `uninstall`. |
| Design claim (findings fix): "anchoring an active program's close on its last non-report commit closes the show-report self-count gap" | The regression written for it: a content commit landed after a trailer-carrying regeneration commit | corrected | The buried regeneration fell back inside the cast range and was counted (2 expected, 3 received). `getCast` now excludes report-only commits as well; the test passed on the next run. Caught before commit. |
| "No report-only commit in the program's range carries a trailer" (first listing, from the CS-00 merge commit `9c3ca28`) | Recounted from the plan's real `baseline:` commit (`1293fae`) | corrected | Exactly one does — `dfbb98d fix(codex-support): generate the program's show-report golden pair`, the CS-00 session's first report commit, written before the trailer-free discipline existed. It is why the regenerated cast reads 32 where the committed report read 33. The first listing had used the wrong lower bound. |
| "A restoring `spell update` leaves real manifest churn uncommitted" (reading `git status` in the CS-08 prototype) | `git diff --ignore-cr-at-eol` (empty) and `git add` ("nothing to commit"); `core.autocrlf` read back as `true` | corrected | A line-ending-only artifact: the manifest is rewritten with LF into a CRLF working copy. It still blocked the next `spell update`, so it is filed as a TODO LOW rather than dismissed. |
| Probe hypothesis: "a subagent spawned after `spell init --user` sees the newly installed personal `spell-status` command" | Spawned one and had it invoke the skill and quote the loaded include line | corrected | It loaded the project copy (`@.arcane/spells/spell-status.md`): the skill list is inherited from the parent session, fixed at its start. User-level Claude Code behavior stays the operator's Q-005; the tier is now installed for it. |
| "Codex discovers skills that are on disk but gitignored" | `git check-ignore` on the restored skill, then `codex exec --cd <restored clone>` asked for a count | confirmed | — (81 `spell-*` skills listed: 40 from the ignored folder, 41 from the user tier; nothing in the trace mentioned ignore rules) |
| "`1.1.1` is published" (the 0.34.3 lesson, applied as a check) | `gh run view` on `publish.yml` run 34501917963, `npm view arcane-cli version`, then the global CLI upgraded and `spell update --user` run against the real store (`Updated 41 files`, `Installed: 1.1.1`) | confirmed | — |
| "The second failed push of the CS-08 branch was the show-report golden gate again" (assumed from the first failure, whose cause it genuinely was) | Re-ran `check:version-bump`, the golden test and the full suite locally (all green), then pushed again unchanged (passed) | unverifiable | The failing test's identity was not captured — the push output was filtered through a pattern that hid vitest's summary. Recorded as an uncaptured under-contention flake, the class `TODO.md`'s "Test Suite" item already describes; the fix was reading the full log the third time, which is also what that item recommends. |

## 2026-09-10/11 — Codex Support CS-05 (repo opt-out, `spell_scope`)

The session that shipped `1.2.0`. Two of the epic's own stated premises were corrected against the
tree before any code was written; four claims the executing agent held while building were corrected
by its own tests or by running the real CLI; two were confirmed.

| Claim | Verification method | Result | Correction |
| --- | --- | --- | --- |
| PLAN.md CS-05: "the migration/prune logic must never silently drop an operator-edited file (ARC-038 hash rule)" — read as prune logic this epic must write and get right | Built the state first: a `lite` consumer beside a stubbed-home user tier, `spell_scope` set by hand, then a hash sweep over all 170 tracked files after one deliberate edit | corrected | There is no prune logic to write. 170/170 files were hash-recorded; 169 matched and 1 did not, and the sweep named the edited one. Opting out turns the files into orphans, and `resolveOrphan` already reports every run and deletes only under `--prune` while the hash holds. The epic's real job was making the `spells-*` components install nothing. |
| The opt-out's value is removing picker clutter across a multi-root workspace (the PRD's framing since CS-00) | Read which file the harness actually expanded for two spell invocations in one session, distinguishable because the project command includes a relative path and the personal command an absolute one | corrected | Sharper than clutter: `/spell-full-cycle` resolved through the **user tier** while `/spell-open-session`, minutes earlier, resolved through the **project** copy. With both tiers owning a name, which copy runs is not predictable from the vendor documentation. One owner removes the question rather than answering it. |
| CS-04's open question, carried as documentation only: a personal command's ABSOLUTE `@` include resolves and loads the canonical body | The same observation — the harness expanded `C:/Users/payini/.arcane/spells/spell-full-cycle.md` and the full body followed | confirmed | — (first live confirmation; the clean-room check stays Q-005, because this one happened as a side effect of a working session rather than as a deliberate test) |
| The plan sketch's "Marker blocks gain one pointer line" is CS-05 work | `grep mergeIntoFile src/` and read the one caller | corrected | The only marker sections Arcane writes carry the **agent roster**, produced by `agent-generator.ts` — there is no spell-routing block for a pointer to live in. Dropped from the epic on the record; agent delivery at the user tier is CS-06's subject. |
| Design claim (own): "`spell update` honours the field, and the removal is the orphan path" — taken as sufficient | Ran the built CLI against the real fixture rather than only the unit tests | corrected | Three gaps, all found this way and all fixed: a same-version run short-circuited on "Already up to date." and never mentioned the newly unmanaged files; switching back to `"repo"` had the same silence in mirror; and `--prune` left 41 empty `.agents/skills/<id>/` directories behind. |
| Design claim (own): "the report-then-prune flow works" — after the report-only run printed its own remedy | Ran that remedy: `spell update --prune` on the next invocation | corrected | It found nothing. The report-only run had dropped the orphans from the manifest, so the remedy it printed was untrue — and a file Arcane wrote had silently stopped being tracked for `spell uninstall`. An orphan still on disk now stays tracked with its recorded hash; `test/update.test.ts`'s assertion encoding the old behaviour was updated on the record. |
| Implicit assumption (own): `spell doctor` completes quickly enough to script an EV-01 check around it | Ran it with a stubbed home; it hung past five minutes, so `execFile("code", ["--version"])` was timed directly | corrected | Not the tier's doing: `checkVSCodeExtension` reads `process.env["HOME"]` only and shells out to `code --version` when the extension directory is absent — measured at >25s before a hard kill, twice per run, and `HOME` is routinely unset on Windows. Filed as a `TODO.md` MEDIUM; EV-01 worked around it. |
| Test claim (own): the new `init` opt-out tests pass | Ran them inside the full suite rather than alone | corrected | One timed out at 5265ms against vitest's 5s default — a real `lite` install (170 files) per test. Budgeted explicitly per test, never a global `testTimeout` bump (Become Current lesson E28). |
| "`1.2.0` is published" (the 0.34.3 lesson, applied as a check rather than assumed) | `gh run watch` on publish run 34569378898, then `npm view arcane-cli version`, then installing `arcane-cli@1.2.0` into a scratch prefix and grepping its bundle | confirmed | — (`npm view` still read `1.1.1` immediately after the workflow succeeded; the check was repeated until the registry served `1.2.0`, and the installed bundle carries `spell_scope`) |
